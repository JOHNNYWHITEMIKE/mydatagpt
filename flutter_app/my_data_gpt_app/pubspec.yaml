name: my_data_gpt_app
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0

  # State
  provider: ^6.1.0

  # Security
  flutter_secure_storage: ^10.0.0-beta.4
  local_auth: ^2.3.0
  encrypt: ^5.0.1
